import PartyButton from "./components/partyButton.js";
import CatButton from "./components/catFactsButton";

document.body.appendChild(new PartyButton().buttonContainer);

document.body.appendChild(new CatButton().buttonContainer);